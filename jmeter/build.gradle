apply plugin: 'com.palantir.docker'

docker {
    dependsOn build
    name 'pedront/rvss-jmeter'
    tags 'latest'
    dockerfile file('Dockerfile')
    files 'apache-jmeter-4.0.tgz', 'test.jmx', 'docker-entrypoint.sh', 'upload.py'
}
